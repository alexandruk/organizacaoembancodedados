CREATE TABLE FUNCIONARIO (
MATRICULA NUMBER (4),
NOME VARCHAR2 (30),
SALARIO NUMBER (7,2),
CONSTRAINT FUNCIONARIO_PK PRIMARY KEY (MATRICULA)
);

INSERT INTO FUNCIONARIO VALUES (1001, 'ANTONIO', 3000);
INSERT INTO FUNCIONARIO VALUES (1002, 'BEATRIZ', 2000);
INSERT INTO FUNCIONARIO VALUES (1003, 'CLAUDIO', '');
INSERT INTO FUNCIONARIO VALUES (1004, 'DANIELA', 5000);
INSERT INTO FUNCIONARIO VALUES (1005, 'ERNESTO', 2000);
INSERT INTO FUNCIONARIO VALUES (1006, 'FABIANA', '');
INSERT INTO FUNCIONARIO VALUES (1007, 'GERALDO', 5000);


Quais os nomes dos funcionários para os quais ainda não foram atribuídos salários?

SELECT NOME FROM FUNCIONARIO WHERE SALARIO IS NULL;




Quais os nomes dos funcionários que recebem salários entre 3000 e 5000?

SELECT NOME FROM FUNCIONARIO WHERE SALARIO BETWEEN 3000 AND 5000;




Quais os nomes dos funcionários que recebem os maiores salários?

SELECT NOME FROM FUNCIONARIO WHERE SALARIO =
(SELECT MAX(SALARIO) FROM FUNCIONARIO);
